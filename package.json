{
  "name": "vivaan-kaushal-site",
  "private": true,
  "description": "Personal site — static pages generated from a folder of markdown.",
  "scripts": {
    "build": "node build.js",
    "serve": "node build.js && npx --yes serve ."
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
